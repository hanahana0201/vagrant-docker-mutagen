sync:
  app:
    mode: "two-way-resolved"
    alpha: "./workspace"
    beta: "my-app:/home/vagrant/workspace"
    ignore:
      vcs: true
      paths:
        - "/node_modules"
        - "/log"
        - "/tmp"